apiVersion: v1
kind: Service
metadata:
  name: service-name
  labels:
    app: rapido-application
  namespace: testing
spec:
  selector: 
    type: LoadBalancer
  ports:
    - name: port-define
      port: 80
      protocol: TCP
    # targetport: 4444